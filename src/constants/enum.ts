export enum Roles {
  HR = "hr",
  CANDIDATE = "candidate",
}

export enum InterviewType {
  AI = "ai",
  HUMAN = "human",
}

export enum InterviewStatus {
  SCHEDULED = "scheduled",
  COMPLETED = "completed",
  CANCELLED = "cancelled"
}

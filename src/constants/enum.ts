export enum Roles {
  HR = "hr",
  CANDIDATE = "candidate",
}

export enum InterviewType {
  AI = "ai",
  HUMAN = "human",
}

export enum InterviewStatus {
  ACTIVE = "active",
  COMPLETE = "complete",
  EXPIRED = "expired",
  INACTIVE = "inactive"
}